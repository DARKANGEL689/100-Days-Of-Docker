apiVersion: v1
kind: Service
metadata:
  name: tensor-sync-svc
  namespace: default
  annotations:
    service.kubernetes.io/topology-mode: "Auto"
spec:
  selector:
    app: tensor-engine
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080