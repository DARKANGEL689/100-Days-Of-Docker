apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: safe-developer-role
  namespace: sandbox
rules:
- apiGroups: ["", "apps"]
  resources: ["pods", "deployments", "services"]
  verbs: ["get", "list", "watch", "create", "update"] 