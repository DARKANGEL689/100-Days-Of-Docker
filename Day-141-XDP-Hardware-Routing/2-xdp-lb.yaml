apiVersion: v1
kind: Service
metadata:
  name: edge-trading-lb
  namespace: default
  annotations:
    service.cilium.io/global: "true"
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: trading-api