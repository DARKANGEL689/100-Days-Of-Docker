apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: random-pod-assassination
  namespace: default
spec:
  action: pod-kill
  mode: one 
  selector:
    labelSelectors:
      "app": "ledger-api"
  scheduler:
    cron: "@every 15s"