apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: backend-fault-injector
spec:
  hosts:
  - backend-svc
  http:
  - fault:
      delay:
        percentage:
          value: 50.0
        fixedDelay: 5s 
    route:
    - destination:
        host: backend-svc