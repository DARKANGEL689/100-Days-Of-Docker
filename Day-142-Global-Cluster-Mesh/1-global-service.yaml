apiVersion: v1
kind: Service
metadata:
  name: enterprise-auth-backend
  namespace: default
  annotations:
    io.cilium/global-service: "true"
    io.cilium/shared-service: "true"
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: global-auth