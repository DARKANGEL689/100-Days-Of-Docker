===================================================
🚀 SUCCESSFULLY DEPLOYED: {{ .Release.Name }}
===================================================
Your application is currently spinning up {{ .Values.replicaCount }} replicas!

To watch your pods boot up, run:
  kubectl get pods -w