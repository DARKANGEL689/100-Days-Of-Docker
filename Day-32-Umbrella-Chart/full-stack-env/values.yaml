nginx:
  replicaCount: 3
  service:
    type: LoadBalancer

redis:
  architecture: standalone
  auth:
    enabled: true
    existingSecret: "my-database-password"
    existingSecretPasswordKey: "db-password"